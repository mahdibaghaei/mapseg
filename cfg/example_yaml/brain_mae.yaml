system:
  project: PROJE
  exp_name: BRAIN_MAE_70
  n_threads: 8
  ckpt_dir: './mae_ckpt'
  seed: 0

train: 
  niter: 200
  niter_decay: 100
  type: 'mae'

data: 
  mae_root: './dataset'

model:
  pretrain_model: None 
